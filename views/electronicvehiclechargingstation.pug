extends layout

block content
  h2 This is ElectronicVehicleChargingStation :)
  table
    for item in results
      tr
        td=item.name
        td=item.address
        td=item.latitude
        td=item.longitude
        td
          a(href='/post?title=electronicVehicleChargingStation&id=' + item.ID)=item.name 
